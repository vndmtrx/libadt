#ifndef _LIBADT_DLIST_DECL_H
#define _LIBADT_DLIST_DECL_H

/*
 * Type declaration for use with imports that require only the type, not
 * the function prototypes. 
 */

struct _dlist_root;

struct _dlist_node;

typedef struct _dlist_root dlist_root;

typedef struct _dlist_node dlist_node;

#endif
